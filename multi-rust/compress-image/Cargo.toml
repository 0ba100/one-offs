[package]
name = "compress-image"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
clap = {version = "4.5.39", features = ["derive"]}
image = {version = "0.25.6", features = ["avif", "webp"]}
